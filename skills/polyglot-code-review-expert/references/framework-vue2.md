# Vue 2 审查清单

适用于 Options API 为主的 Vue 2 项目。

## 重点检查

- `data`、`computed`、`watch` 的职责是否混乱。
- 对对象/数组的修改是否符合 Vue 2 响应式限制。
- 组件通信是否过度依赖事件链或全局总线。
- `watch` 是否承担了本应放在计算属性或服务层的逻辑。
- 生命周期时机是否正确，销毁时是否清理监听器和定时器。

## 常见误区

- 直接修改深层对象但未触发响应式。
- 通过多个 watcher 串出隐式状态机。
