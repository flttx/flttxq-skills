# Electron 审查清单

适用于主进程 / 渲染进程 / preload 分层的桌面项目。

## 重点检查

- 主进程、渲染进程、preload 的职责边界是否清晰。
- IPC 通道是否做了输入校验与最小暴露。
- 是否错误地打开了 `nodeIntegration`、关闭了 `contextIsolation`。
- 文件系统、shell、剪贴板、系统命令调用是否存在安全风险。
- 自动更新、日志、异常恢复是否能覆盖生产场景。

## 常见误区

- 直接在渲染进程暴露危险能力。
- 把业务协议和系统能力混在同一 IPC 层。
